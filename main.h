#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdlib.h>
#include <string.h>
#include <stddef.h>

int _size(unsigned int nb);
int _putnbr(int n);
int _printf(const char *format, ...);

#endif
